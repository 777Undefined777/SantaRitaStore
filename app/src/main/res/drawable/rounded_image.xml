<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="2dp"
        android:color="@color/black" /> <!-- Cambia el color del borde según tus preferencias -->
    <size android:width="72dp" android:height="72dp" /> <!-- Ajusta el tamaño del borde al tamaño del ImageView -->
</shape>
